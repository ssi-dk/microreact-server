import React from 'react';
// import { Timeline } from 'react-twitter-widgets';

const TwitterFeed = () => (
  <div className="mr-twitter-feed">
    {/*
    <Timeline
      dataSource={{
        sourceType: 'profile',
        screenName: 'mymicroreact',
      }}
      options={{
        username: 'MyMicroreact',
        height: '640',
      }}
    />
    */}
  </div>
);

export default TwitterFeed;
